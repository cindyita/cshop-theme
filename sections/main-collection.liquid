
<style>
  .productsall {
    background-color: #f0ede6;
    width: 100%;
    height: 880px;
    padding-top:150px;
  }
  .productdisplay {
    background-color: white;
    padding:20px 50px 35px;
    box-shadow: 0px 0px 5px 1px rgba(0, 0, 0,0.3);
    margin:20px;
  }
  a{
    text-decoration:none;
    color:black;
  }
  a:hover {
    text-decoration:none;
    color:black;
  }
</style>




    <div class="productsall">
      <div class="d-flex justify-content-center">
        
        {% paginate collection.products by 2 %}
        {% for product in collection.products %}
        
        <a href="{{ product.url | within: collection }}">
          <div class="productdisplay"><center>
          <img src="{{ product.featured_image.src | img_url: 'large' }}" width="150px" alt="{{ product.featured_image.alt | escape }}">
          <h3>{{ product.title }}</h3>
          <p> {{ product.price | money }}</p>
           {% unless product.available %}<br><strong>sold out</strong>{% endunless %}
          <p>Mezcla de molleja e hígado de pollo junto con lenteja, <br>
            manzana y zanahoria para una fuente de vitaminas y fibra. </p>
          <button class="btn btn-warning">Ver producto</button>
       	 </center></div>
        </a>
        
        {% else %}
          <p>no matches</p>
        {% endfor %}
        {% if paginate.pages > 1 %}
          {{ paginate | default_pagination }}
        {% endif %}
      {% endpaginate %}
        
      </div>
    </div>